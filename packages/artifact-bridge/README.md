# artifact-bridge 0.1.0

Optional progressive-enhancement protocol. Artifacts remain functional when no host
exists. Consumers pin and receive this package through `inline`, `vendor`, or `bundle`
delivery; they never import it from a hub path.
